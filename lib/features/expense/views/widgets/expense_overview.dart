import 'package:flutter/material.dart';

class ExpenseOverview extends StatelessWidget {
  const ExpenseOverview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Expense Overview');
  }
}
