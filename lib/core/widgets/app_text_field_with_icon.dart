import 'package:flutter/material.dart';

class AppTextFieldWithIcon extends StatelessWidget {
  const AppTextFieldWithIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
